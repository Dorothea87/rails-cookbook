<h1>All my recipe categories inmy cookbook are here:</h1>
  <div>
    <ul>
    <% @categories.each do |category| %>
      <li><%= link_to category.name, category_path(category) %></li>
      <li><%= link_to category.name, category_path(category), data: {turbo_method: :delete, turbo_confirm: "Are you sure you want to delete this?"} %></li>
    <% end %>
    </ul>
  </div>
